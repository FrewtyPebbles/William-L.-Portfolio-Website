{
  "name": "portfolio-website",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c cyan,green \"cd frontend && npm run dev\" \"cd backend && ..\\.venv\\Scripts\\uvicorn app.main:app --reload --port 8000\"",
    "build": "cd frontend && npm run build",
    "deploy": "node deploy.mjs",
    "lint": "cd frontend && npx tsc --noEmit"
  },
  "devDependencies": {
    "@types/react-syntax-highlighter": "^15.5.13",
    "concurrently": "^9.2.1"
  }
}
